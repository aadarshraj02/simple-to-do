const TodoCard = () => {
  return (
    <div>
      <li>
        <div>
          <button>Update</button>
          <button>Delete</button>
        </div>
      </li>
    </div>
  );
};

export default TodoCard;
